import { NextRequest } from "next/server";
import { z } from "zod";
import { db } from "@/lib/db";
import { ApiError, apiError, apiSuccess } from "@/lib/api";
import { getGuestSession, getOrCreateCart, calculateDiscount, recordAnalytics } from "@/lib/storefront";

export const runtime = "nodejs";
const schema = z.object({ name: z.string().trim().min(2).max(100), email: z.string().email().max(200).optional().or(z.literal("")), phone: z.string().trim().min(7).max(30), state: z.string().trim().min(2).max(80), city: z.string().trim().min(2).max(80), address: z.string().trim().min(5).max(500), notes: z.string().trim().max(1000).optional(), coupon: z.string().trim().max(40).optional() });

export async function POST(request: NextRequest) { try { const body = schema.parse(await request.json()); const session = await getGuestSession(); const cart = await getOrCreateCart(session); if (!cart.items.length) throw new ApiError("EMPTY_CART", "Your cart is empty", 400); const productIds = cart.items.map(i => i.productId); const products = await db.product.findMany({ where: { id: { in: productIds }, published: true, status: "PUBLISHED" }, include: { variants: true } }); const lines = cart.items.map(item => { const p = products.find(x => x.id === item.productId); if (!p) throw new ApiError("PRODUCT_UNAVAILABLE", "A product in your cart is unavailable", 409); const v = item.variantId ? p.variants.find(x => x.id === item.variantId && x.active) : null; const price = v?.price ?? p.price; const stock = v?.stock ?? p.stock; if (stock < item.quantity) throw new ApiError("OUT_OF_STOCK", `${p.name} does not have enough stock`, 409); return { item, p, v, price, total: price * item.quantity }; }); const subtotal = lines.reduce((s, l) => s + l.total, 0); let couponRecord = null; if (body.coupon) { couponRecord = await db.coupon.findUnique({ where: { code: body.coupon.toUpperCase() } }); if (!couponRecord || !couponRecord.active || (couponRecord.expiresAt && couponRecord.expiresAt < new Date()) || (couponRecord.usageLimit !== null && couponRecord.usageCount >= couponRecord.usageLimit) || (couponRecord.minimumOrder !== null && subtotal < couponRecord.minimumOrder)) throw new ApiError("INVALID_COUPON", "Coupon is invalid or unavailable", 400); } const zone = await db.deliveryZone.findFirst({ where: { state: { equals: body.state, mode: "insensitive" }, city: { equals: body.city, mode: "insensitive" }, active: true } }); if (!zone) throw new ApiError("DELIVERY_UNAVAILABLE", "Delivery is not available for this location yet", 400); const shipping = zone.freeDeliveryThreshold !== null && subtotal >= zone.freeDeliveryThreshold ? 0 : zone.fee; const discount = calculateDiscount(subtotal, couponRecord ? { type: couponRecord.type, value: couponRecord.value } : undefined); const total = subtotal + shipping - discount; const customer = await db.customer.upsert({ where: { email: body.email || `guest-${session}@enkays.local` }, update: { name: body.name, phone: body.phone }, create: { name: body.name, email: body.email || `guest-${session}@enkays.local`, phone: body.phone } }); const order = await db.$transaction(async tx => { const created = await tx.order.create({ data: { orderNumber: `${(await tx.siteSettings.findUnique({ where: { id: 1 } }))?.orderPrefix ?? "ENK"}-${Date.now().toString(36).toUpperCase()}`, customerId: customer.id, customerName: body.name, customerEmail: body.email || null, customerPhone: body.phone, subtotal, shipping, discount, total, deliveryMethod: zone.pickup ? "DELIVERY_OR_PICKUP" : "DELIVERY", deliveryAddress: `${body.address}, ${body.city}, ${body.state}`, notes: body.notes, items: { create: lines.map(l => ({ productId: l.p.id, productNameSnapshot: l.p.name, skuSnapshot: l.v?.sku ?? l.p.sku, quantity: l.item.quantity, unitPrice: l.price, total: l.total, variantSnapshot: l.v ? { id: l.v.id, name: l.v.name, value: l.v.value } : undefined })) } } }); if (couponRecord) { await tx.coupon.update({ where: { id: couponRecord.id }, data: { usageCount: { increment: 1 } } }); await tx.couponUsage.create({ data: { couponId: couponRecord.id, customerId: customer.id, orderId: created.id } }); } for (const line of lines) { await tx.product.update({ where: { id: line.p.id }, data: { stock: { decrement: line.item.quantity } } }); if (line.v) await tx.productVariant.update({ where: { id: line.v.id }, data: { stock: { decrement: line.item.quantity } } }); } await tx.cartItem.deleteMany({ where: { cartId: cart.id } }); return created; }); await recordAnalytics("ORDER_CREATED", { customerId: customer.id, sessionId: session, metadata: { orderId: order.id, total } }); return apiSuccess(order); } catch (e) { return apiError(e); } }
