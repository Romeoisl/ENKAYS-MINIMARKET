"use client";

import { FormEvent, useRef, useState } from "react";
import { useRouter } from "next/navigation";
import { toast } from "sonner";

type Product = { id?: string; name: string; slug: string; description: string; shortDescription?: string | null; price: number; compareAtPrice?: number | null; currency: string; sku: string; stock: number; featured: boolean; published: boolean; categoryId?: string | null; brandId?: string | null; images?: { id: string; secureUrl: string; alt?: string | null }[] };
type Option = { id: string; name: string };
function initialProduct(product?: Product): Product { return product ?? { name: "", slug: "", description: "", shortDescription: "", price: 0, compareAtPrice: null, currency: "NGN", sku: "", stock: 0, featured: false, published: false, categoryId: null, brandId: null, images: [] }; }

export function ProductForm({ product, categories = [], brands = [] }: { product?: Product; categories?: Option[]; brands?: Option[] }) {
  const router = useRouter(); const fileRef = useRef<HTMLInputElement>(null); const [form, setForm] = useState(initialProduct(product)); const [busy, setBusy] = useState(false); const [uploading, setUploading] = useState(false);
  const set = (key: keyof Product, value: unknown) => setForm((v) => ({ ...v, [key]: value }));
  async function submit(event: FormEvent) { event.preventDefault(); setBusy(true); try { const payload = { name: form.name, slug: form.slug, description: form.description, shortDescription: form.shortDescription || null, price: Number(form.price), compareAtPrice: form.compareAtPrice == null ? null : Number(form.compareAtPrice), currency: form.currency, sku: form.sku, stock: Number(form.stock), featured: form.featured, published: form.published, categoryId: form.categoryId || null, brandId: form.brandId || null }; const response = await fetch(form.id ? `/api/v1/admin/products/${form.id}` : "/api/v1/admin/products", { method: form.id ? "PUT" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) }); const json = await response.json(); if (!response.ok) throw new Error(json?.error?.message ?? "Could not save product"); toast.success("Product saved"); router.push(`/admin/products/${json.data.id}`); router.refresh(); } catch (e) { toast.error(e instanceof Error ? e.message : "Could not save product"); } finally { setBusy(false); } }
  async function upload() { if (!form.id) { toast.error("Save the product before uploading images"); return; } const file = fileRef.current?.files?.[0]; if (!file) return; setUploading(true); try { const body = new FormData(); body.append("file", file); body.append("productId", form.id); const response = await fetch("/api/v1/admin/media/upload", { method: "POST", body }); const json = await response.json(); if (!response.ok) throw new Error(json?.error?.message ?? "Upload failed"); setForm((v) => ({ ...v, images: [...(v.images ?? []), json.data] })); toast.success("Image uploaded"); if (fileRef.current) fileRef.current.value = ""; } catch (e) { toast.error(e instanceof Error ? e.message : "Upload failed"); } finally { setUploading(false); } }
  return <form onSubmit={submit} className="space-y-6"><section className="rounded-2xl border border-ink-100 bg-white p-6 shadow-sm"><div className="grid gap-5 md:grid-cols-2"><label className="space-y-2 md:col-span-2"><span className="text-sm font-semibold">Product name</span><input required value={form.name} onChange={(e) => set("name", e.target.value)} className="w-full rounded-xl border border-ink-200 px-3 py-2.5" /></label><label className="space-y-2"><span className="text-sm font-semibold">Slug</span><input required value={form.slug} onChange={(e) => set("slug", e.target.value.toLowerCase().replace(/\s+/g, "-"))} className="w-full rounded-xl border border-ink-200 px-3 py-2.5" /></label><label className="space-y-2"><span className="text-sm font-semibold">SKU</span><input required value={form.sku} onChange={(e) => set("sku", e.target.value)} className="w-full rounded-xl border border-ink-200 px-3 py-2.5" /></label><label className="space-y-2"><span className="text-sm font-semibold">Price (₦)</span><input required min="0" type="number" value={form.price / 100} onChange={(e) => set("price", Math.round(Number(e.target.value) * 100))} className="w-full rounded-xl border border-ink-200 px-3 py-2.5" /></label><label className="space-y-2"><span className="text-sm font-semibold">Compare-at price (₦)</span><input min="0" type="number" value={form.compareAtPrice ? form.compareAtPrice / 100 : ""} onChange={(e) => set("compareAtPrice", e.target.value ? Math.round(Number(e.target.value) * 100) : null)} className="w-full rounded-xl border border-ink-200 px-3 py-2.5" /></label><label className="space-y-2"><span className="text-sm font-semibold">Stock</span><input required min="0" type="number" value={form.stock} onChange={(e) => set("stock", Number(e.target.value))} className="w-full rounded-xl border border-ink-200 px-3 py-2.5" /></label><label className="space-y-2"><span className="text-sm font-semibold">Category</span><select value={form.categoryId ?? ""} onChange={(e) => set("categoryId", e.target.value || null)} className="w-full rounded-xl border border-ink-200 bg-white px-3 py-2.5"><option value="">No category</option>{categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="space-y-2"><span className="text-sm font-semibold">Brand</span><select value={form.brandId ?? ""} onChange={(e) => set("brandId", e.target.value || null)} className="w-full rounded-xl border border-ink-200 bg-white px-3 py-2.5"><option value="">No brand</option>{brands.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="space-y-2 md:col-span-2"><span className="text-sm font-semibold">Short description</span><input value={form.shortDescription ?? ""} onChange={(e) => set("shortDescription", e.target.value)} className="w-full rounded-xl border border-ink-200 px-3 py-2.5" /></label><label className="space-y-2 md:col-span-2"><span className="text-sm font-semibold">Description</span><textarea required rows={7} value={form.description} onChange={(e) => set("description", e.target.value)} className="w-full rounded-xl border border-ink-200 px-3 py-2.5" /></label></div><div className="mt-5 flex flex-wrap gap-6"><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.featured} onChange={(e) => set("featured", e.target.checked)} /> Featured</label><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.published} onChange={(e) => set("published", e.target.checked)} /> Published</label></div></section><section className="rounded-2xl border border-ink-100 bg-white p-6 shadow-sm"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><h2 className="font-bold">Product media</h2><p className="mt-1 text-sm text-ink-500">JPEG, PNG, WebP or AVIF · max 8 MB.</p></div><input ref={fileRef} type="file" accept="image/jpeg,image/png,image/webp,image/avif" className="max-w-[260px] text-sm" onChange={() => void upload()} disabled={!form.id || uploading} /></div>{(form.images ?? []).length > 0 && <div className="mt-5 grid grid-cols-2 gap-4 sm:grid-cols-4">{form.images?.map((image) => <div key={image.id} className="overflow-hidden rounded-xl border border-ink-100"><img src={image.secureUrl} alt={image.alt ?? form.name} className="aspect-square w-full object-cover" /></div>)}</div>}</section><div className="flex justify-end gap-3"><button type="button" onClick={() => router.push("/admin/products")} className="rounded-xl border border-ink-200 px-5 py-2.5 text-sm font-semibold">Cancel</button><button disabled={busy || uploading} className="rounded-xl bg-enkays-700 px-5 py-2.5 text-sm font-semibold text-white disabled:opacity-50">{busy ? "Saving…" : "Save product"}</button></div></form>;
}
